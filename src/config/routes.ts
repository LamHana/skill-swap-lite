const routes = {
  home: '/',
  login: '/login',
  register: '/register',
  profile: '/profile',
  chat: '/chat',
  myNetwork: '/my-network',
  notFound: '*',
  landing: '/landing',
};

export default routes;
