const routes = {
  home: '/',
  login: '/login',
  register: '/register',
  profile: '/profile',
  myNetwork: '/my-network',
  editProfile: '/edit-profile',
  notFound: '*',
};

export default routes;
